#!/usr/bin/env bash

# Commands to run



# Comments
# Compare and contrast the argument and the environment variables from the two times we ran `echoscript.sh`.

# Compare and contrast the differences when you built/ran the docker container with and without the compose changing values.